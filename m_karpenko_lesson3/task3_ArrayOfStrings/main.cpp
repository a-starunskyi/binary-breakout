#include <iostream>
#include <string>
using namespace std;

int main() {
    // TODO:
    // 1. Declare an array of strings with values "Apple", "Banana", "Cherry"
    
    string fruit[3] = { "Apple", "Banana", "Cherry" };

    // 2. Print the first string from the array (remember, array indexing starts from 0)

    cout << "First string from the array: " << fruit[0];
 
    return 0;
}