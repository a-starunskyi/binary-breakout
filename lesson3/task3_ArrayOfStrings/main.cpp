#include <iostream>
#include <string>

int main() {
    // TODO:
    // 1. Declare an array of strings with values "Apple", "Banana", "Cherry"
    std::string stringArray[3] = { Apple,Banana,Cherry };
    // 2. Print the first string from the array (remember, array indexing starts from 0)
    std::cout << "First string of the array = " << stringArray[0];
    return 0;
} 